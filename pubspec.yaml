name: audiovault_editor
description: AudioVault Editor - Audiobook metadata editor.
publish_to: 'none'
version: 1.1.0+2

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^11.0.0
  path: ^1.9.1
  audio_metadata_reader: ^1.4.2
  xml: ^6.0.0
  image: ^4.5.0
  desktop_drop: ^0.4.4
  window_manager: ^0.4.0
  shared_preferences: ^2.3.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
